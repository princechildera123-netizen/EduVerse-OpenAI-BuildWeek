import type { Flashcard, PlannerItem, QuizPrompt } from '../types/studyCoach';

export function PlannerCard({ title, items }: { title: string; items: PlannerItem[] }) { return <section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="text-xl font-black text-ink">{title}</h2><div className="mt-4 grid gap-3">{items.map((item) => <article key={`${item.title}-${item.time}`} className="rounded-2xl bg-slate-50 p-4"><div className="flex justify-between gap-3"><h3 className="font-bold text-ink">{item.title}</h3><span className="text-sm font-bold text-brand">{item.time}</span></div><p className="mt-1 text-sm text-muted">{item.detail}</p></article>)}</div></section>; }
export function FlashcardsSection({ cards }: { cards: Flashcard[] }) { return <section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="text-xl font-black text-ink">AI Flashcards</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{cards.map((card) => <article key={card.front} className="rounded-2xl border border-brand/10 bg-brand/5 p-4"><p className="font-bold text-brand">{card.front}</p><p className="mt-2 text-sm text-muted">{card.back}</p></article>)}</div></section>; }
export function QuizGeneratorSection({ prompts }: { prompts: QuizPrompt[] }) { return <section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="text-xl font-black text-ink">AI Quiz Generator</h2><div className="mt-4 grid gap-3">{prompts.map((prompt) => <article key={prompt.question} className="rounded-2xl bg-slate-50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-brand">{prompt.skill}</p><p className="mt-1 font-semibold text-ink">{prompt.question}</p></article>)}</div></section>; }
export function TimerCard() { return <section className="rounded-3xl bg-gradient-to-br from-ink to-brand p-6 text-white shadow-xl shadow-brand/20"><p className="font-semibold text-white/70">Study timer</p><strong className="mt-3 block text-5xl">25:00</strong><p className="mt-3 text-white/75">Focus mode ready. Notifications muted during deep work.</p></section>; }
export function ProductivityTracker() { return <section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="text-xl font-black text-ink">Productivity tracker</h2><div className="mt-4 grid grid-cols-3 gap-3 text-center"><strong className="rounded-2xl bg-slate-50 p-3 text-brand">4/5<br /><span className="text-xs text-muted">Goals</span></strong><strong className="rounded-2xl bg-slate-50 p-3 text-brand">82%<br /><span className="text-xs text-muted">Energy</span></strong><strong className="rounded-2xl bg-slate-50 p-3 text-brand">Low<br /><span className="text-xs text-muted">Distraction</span></strong></div></section>; }
