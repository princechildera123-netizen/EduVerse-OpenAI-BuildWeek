import { ActionCard, DashboardHero, DashboardSection, MetricCard, ProgressBar, SimpleList } from '../shared-dashboard/components/DashboardPrimitives';
import { teacherActions, teacherAnalytics, teacherLists, teacherMetrics } from './data/teacherDashboardData';

export function TeacherDashboardPage() {
  return <div className="py-8 sm:py-12"><DashboardHero eyebrow="Teacher Dashboard" title="Welcome back, Ms. Rivera." description="Monitor classroom momentum, manage assignments, and use AI teaching support to plan the next best instructional move." /><section className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{teacherMetrics.map((metric) => <MetricCard key={metric.label} {...metric} />)}</section><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_22rem]"><main className="grid gap-6"><DashboardSection title="Class overview"><SimpleList items={teacherLists.classes} /></DashboardSection><DashboardSection title="Student performance summary"><SimpleList items={teacherLists.performance} /></DashboardSection><DashboardSection title="Assignment management"><SimpleList items={teacherLists.assignments} /></DashboardSection><DashboardSection title="Upcoming lessons"><SimpleList items={teacherLists.lessons} /></DashboardSection><DashboardSection title="Weekly classroom analytics"><div className="grid gap-4">{teacherAnalytics.map((item) => <ProgressBar key={item.label} {...item} />)}</div></DashboardSection></main><aside className="grid content-start gap-6"><DashboardSection title="Attendance overview"><ProgressBar label="Present today" value={94} /></DashboardSection><DashboardSection title="Students needing attention"><SimpleList items={teacherLists.attention} /></DashboardSection><DashboardSection title="AI Teaching Assistant"><div className="grid gap-3">{teacherActions.map((action) => <ActionCard key={action.title} {...action} />)}</div></DashboardSection><DashboardSection title="Notifications"><SimpleList items={teacherLists.notifications} /></DashboardSection></aside></div></div>;
}
