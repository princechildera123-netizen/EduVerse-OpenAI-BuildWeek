import type { ExamPrediction, PredictionDetail } from '../types/prediction';

export function PredictionMetricCard({ label, value, suffix = '%' }: { label: string; value: number; suffix?: string }) { return <article className="rounded-3xl bg-white p-5 shadow-sm"><p className="text-sm font-semibold text-muted">{label}</p><strong className="mt-2 block text-4xl text-ink">{value}{suffix}</strong><div className="mt-4 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-gradient-to-r from-brand to-aqua" style={{ width: `${value}%` }} /></div></article>; }
export function TopicList({ title, topics, tone }: { title: string; topics: string[]; tone: 'weak' | 'strong' }) { return <section className="rounded-3xl bg-white p-5 shadow-sm"><h3 className="font-black text-ink">{title}</h3><div className="mt-3 flex flex-wrap gap-2">{topics.map((topic) => <span key={topic} className={`rounded-full px-3 py-1 text-sm font-semibold ${tone === 'strong' ? 'bg-emerald-50 text-emerald-700' : 'bg-orange-50 text-orange-700'}`}>{topic}</span>)}</div></section>; }
export function DetailCard({ title, detail }: { title: string; detail: PredictionDetail }) { return <article className="rounded-3xl border border-slate-100 bg-white p-5"><h3 className="font-black text-ink">{title}</h3><dl className="mt-4 grid gap-3 text-sm"><div><dt className="font-bold text-brand">Why?</dt><dd className="text-muted">{detail.why}</dd></div><div><dt className="font-bold text-brand">Evidence</dt><dd className="text-muted">{detail.evidence}</dd></div><div><dt className="font-bold text-brand">Recommended Action</dt><dd className="text-muted">{detail.recommendedAction}</dd></div></dl></article>; }
export function RiskBar({ label, value }: { label: string; value: number }) { return <div><div className="flex justify-between text-sm"><span className="font-bold text-ink">{label}</span><span className="font-bold text-brand">{value}%</span></div><div className="mt-2 h-3 rounded-full bg-slate-100"><div className="h-full rounded-full bg-gradient-to-r from-sun to-orange-500" style={{ width: `${value}%` }} /></div></div>; }
export function PredictionSummary({ prediction }: { prediction: ExamPrediction }) { return <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><PredictionMetricCard label="Predicted score" value={prediction.predictedScore} /><PredictionMetricCard label="Confidence" value={prediction.confidence} /><PredictionMetricCard label="Pass probability" value={prediction.passProbability} /><article className="rounded-3xl bg-white p-5 shadow-sm"><RiskBar label="Burnout risk" value={prediction.burnoutRisk} /><div className="mt-5"><RiskBar label="Dropout risk" value={prediction.dropoutRisk} /></div></article></section>; }
